# Ensure platform-specific optional deps (lightningcss, rollup native binaries) install correctly.
# Workaround for https://github.com/npm/cli/issues/4828
legacy-peer-deps=false
optional=true
fund=false
audit=false
